export async function register() {
  // The localStorage polyfill is applied via NODE_OPTIONS in package.json
  // (scripts/polyfill-localstorage.mjs) which runs before any module loads.
  // This instrumentation hook is kept as a no-op placeholder.
}
